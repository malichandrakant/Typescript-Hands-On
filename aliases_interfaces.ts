type Person={
    name:string;
    age:number;
}

let user:Person={
    name:"Rahul",
    age:22
};

// interface

interface Person2{
    name:string;
    age:number;
}

let user2:Person2={
    name:"Anjali",
    age:25
};

console.log(user);
console.log(user2);